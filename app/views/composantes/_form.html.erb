<%= form_for(@composante) do |f| %>
  <% if @composante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@composante.errors.count, "error") %> prohibited this composante from being saved:</h2>

      <ul>
      <% @composante.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :libelle %><br />
    <%= f.text_field :libelle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
